const { length } = "JS";

console.log(length); // 2

//* Bcoz of boxing whenever we try to access some prop/method on primitive value JS BTS create a object respect to their data type and access the prop/method from its prototype and returns the value and after returning the value it destroy them and garbage collector collect them.
