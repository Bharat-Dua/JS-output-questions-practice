let { x, x: y } = { x: 2 };

console.log("x=", x); // 2
console.log("y=", y); // 2

//* Note:- In JS we can destructure the same key as many as times but we have to give alter name on every use we can't use same key again.
