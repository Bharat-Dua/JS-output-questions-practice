
console.log((3)["toString"].length + 1); // 2 bcoz here we are not calling just accessing the method property and The length property of a function returns the number of arguments the function expects. The toString method of a Number object expects one optional argument (the radix), so 3.toString.length is 1.

//* function.length returns count of arguments
