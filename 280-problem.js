var name = 2024;

console.log(name + 1); // '20241'

//* bcoz in window object there is a reserved keyword with name property so if we try to create a variable in global scope with name and whatever we pass to that variable will gets converted into string.
