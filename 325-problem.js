let x = "learn";

console.log(x.substring(5, 1)); // 'earn'

//* Substring : takes two parameters, start and end index. It returns a string that includes the characters from the start index up to but not including the end index. if the start index is greater than end index it will automatically swap them.
