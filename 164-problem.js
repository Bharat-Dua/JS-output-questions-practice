const show = (b, b) => {};
show(); // SyntaxError: Duplicate parameter name not allowed in this context

function test(a, a) {}
test();

//* Note :- In a regular function we can have duplicate param name ,but in arrow function we can't have .
