const arr = [1, 2, 3];
console.log(arr[5]); // undefined bcoz as we know array is also object in js and here we are trying to access the key at certain index and if we try to access the key which doesn't exist in obj we get undefined.
