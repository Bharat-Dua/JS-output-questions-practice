function init(x, y, z) {}

function end(a, b = 0, c) {}

console.log(init.length); //3
console.log(end.length); // 1

//* Note :- if we give any param default value if doesn't cound and after that param element also.
